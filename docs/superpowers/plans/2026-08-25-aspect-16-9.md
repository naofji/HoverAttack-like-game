# 画面比 16:9 対応 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 内部解像度を 1024×768 (4:3) から 1366×768 (16:9) に置き換え、索敵を「横だけ画面幅に比例する楕円」にして、横の力関係も縦の力関係も 4:3 のまま保存する。

**Architecture:** 索敵は現在すべてユークリッド距離の円（`dist < sightRange`）。これを `withinSight(dx, dy, range)` という楕円判定に置き換える。楕円の横半径は渡された `range` そのもの（＝既存の `CANVAS_WIDTH * k`）で、縦半径は `range * SIGHT_ASPECT`。`SIGHT_ASPECT = SIGHT_VERTICAL_BASE / CANVAS_WIDTH` は 4:3 では厳密に 1.0 になり、楕円が真円に退化する。**この性質のおかげで、`CANVAS_WIDTH` を 1024 のまま置き換え作業を完了でき、既存テストが全部緑のままであることが「挙動が 1 ドットも変わっていない」証明になる。** 発砲のゲートは索敵だけ（別途の射程チェックが無い）ため、索敵の横半径を伸ばした分、弾の寿命も同じ倍率で伸ばす。

**Tech Stack:** バニラ ES modules ＋ canvas。ビルド工程なし、依存パッケージなし。テストは `node --test`（DOM も AudioContext も無い）。

**Spec:** `docs/superpowers/specs/2026-08-25-aspect-16-9-design.md`

## Global Constraints

- 内部解像度は **1366×768**。`CANVAS_HEIGHT = 768` は変えない
- `SIGHT_VERTICAL_BASE = 1024`（4:3 時代の幅。索敵の縦半径の基準）
- `SIGHT_ASPECT = SIGHT_VERTICAL_BASE / CANVAS_WIDTH` — 4:3 で 1.0、16:9 で 0.7496
- 拡大率 `1366 / 1024 = 1.3340`
- **`Constants.js` の索敵の表（`sightRange: CANVAS_WIDTH * 0.4` など）と export 名は 1 行も変えない**。`withinSight` が既存の `sightRange` を横半径としてそのまま受け取る
- **`git add -A` / `git add .` は使わない。** 変更したファイルを明示して add する。`src/js/main.js` にはユーザーのデバッグ用 `debugStartMission: 6` が意図的に未コミットで置かれている（本番値は 0）
- **コメントは日本語で「なぜそうしたか」を書く。** 数値を決めたらその根拠（実測値・経緯）を残す
- **ソース文字列を grep するテストは書かない。** 到達不能でも通ってしまうため
- 全テスト: `npm test`（1 秒弱）。1 ファイル: `npm test -- tests/xxx.test.js`
- **Task 1〜6 では `CANVAS_WIDTH` を 1024 のまま据え置く。** Task 7 で初めて 1366 にする。混ぜると「楕円化のバグ」と「画面比の影響」が区別できなくなる

## File Structure

**新規作成:**
- `tests/sight-ellipse.test.js` — `withinSight` の単体テスト（真円への退化 / 楕円の形）
- `tests/aspect-16-9.test.js` — 16:9 側の不変条件（横比の保存・縦絶対値の保存・射程カバー）

**変更:**
- `src/js/utils/Physics.js` — `withinSight` を新設（`hasLineOfSight` の隣）
- `src/js/utils/Constants.js` — `SIGHT_VERTICAL_BASE` / `SIGHT_ASPECT` 追加、`CANVAS_WIDTH` 変更、弾の寿命 4 定数
- `src/js/entities/EnemyTurret.js` — 索敵判定
- `src/js/entities/EnemyDrone.js` — 索敵判定 ＋ 緊急防衛の OR
- `src/js/entities/EnemyTank.js` — 索敵判定（順位付けと分離）
- `src/js/entities/EnemyAttacker.js` — 索敵判定 ＋ 緊急防衛の OR
- `src/js/entities/EnemyBase.js` — `_findTarget` の距離判定
- `src/style.css` — 黒帯計算の比
- `index.html` — canvas の width/height 属性

---

### Task 1: `withinSight` を `utils/Physics.js` に新設する

楕円の索敵判定を、まだ誰も使わない状態で作る。4:3（`SIGHT_ASPECT === 1`）で真円に退化することをテストで押さえる。

**なぜ `utils/Physics.js` か:** `hasLineOfSight` がここにあり、索敵の呼び出し側 5 箇所は既にこのファイルから import している。`utils/geo.js` は国コードとフラグ絵文字のファイルで幾何とは無関係なので、混同しないこと。

**Files:**
- Modify: `src/js/utils/Constants.js`（`CANVAS_HEIGHT` の直後、6 行目あたり）
- Modify: `src/js/utils/Physics.js`（`hasLineOfSight` の直後、117 行目あたり）
- Test: `tests/sight-ellipse.test.js`（新規）

**Interfaces:**
- Consumes: なし
- Produces:
  - `SIGHT_VERTICAL_BASE: number`（`Constants.js`、値 1024）
  - `SIGHT_ASPECT: number`（`Constants.js`、値 `SIGHT_VERTICAL_BASE / CANVAS_WIDTH`）
  - `withinSight(dx: number, dy: number, range: number): boolean`（`Physics.js`）

- [ ] **Step 1: 失敗するテストを書く**

`tests/sight-ellipse.test.js` を新規作成:

```js
import { test } from 'node:test';
import assert from 'node:assert/strict';
import { withinSight } from '../src/js/utils/Physics.js';
import { CANVAS_WIDTH, SIGHT_VERTICAL_BASE, SIGHT_ASPECT } from '../src/js/utils/Constants.js';

// --- 真円への退化 ---
// 4:3 (CANVAS_WIDTH === SIGHT_VERTICAL_BASE) では SIGHT_ASPECT が厳密に 1.0 に
// なり、楕円が真円に退化する。移行でバランスが 1 ドットも動いていないことは、
// 最終的にはこの性質に依っている。

test('SIGHT_ASPECT is the ratio of the 4:3 base width to the current width', () => {
  assert.equal(SIGHT_ASPECT, SIGHT_VERTICAL_BASE / CANVAS_WIDTH);
});

test('at SIGHT_ASPECT === 1 the ellipse is exactly the old circle', (t) => {
  if (SIGHT_ASPECT !== 1) {
    t.skip('16:9 では真円ではない。退化の検査は下の explicit-aspect 版で行う');
    return;
  }
  const range = 410;
  // 境界をまたぐ格子点で総当たりする。円と楕円が 1 点でも食い違えば落ちる。
  for (let dx = -500; dx <= 500; dx += 7) {
    for (let dy = -500; dy <= 500; dy += 7) {
      const circle = Math.hypot(dx, dy) < range;
      assert.equal(
        withinSight(dx, dy, range), circle,
        `dx=${dx} dy=${dy} で円と楕円が食い違った`
      );
    }
  }
});

// --- 楕円の形 ---

test('the horizontal radius is the range itself', () => {
  const range = 546;
  assert.equal(withinSight(range - 1, 0, range), true);
  assert.equal(withinSight(range + 1, 0, range), false);
});

test('the vertical radius shrinks by SIGHT_ASPECT', () => {
  const range = 546;
  const ry = range * SIGHT_ASPECT;
  assert.equal(withinSight(0, ry - 1, range), true);
  assert.equal(withinSight(0, ry + 1, range), false);
});

test('Infinity range accepts everything (EnemyBase._findTarget default)', () => {
  assert.equal(withinSight(99999, 99999, Infinity), true);
});
```

- [ ] **Step 2: 落ちることを確かめる**

Run: `npm test -- tests/sight-ellipse.test.js`
Expected: FAIL。`withinSight` が export されていないので import エラー、
または `SIGHT_ASPECT` が undefined。

- [ ] **Step 3: `Constants.js` に基準値を足す**

`src/js/utils/Constants.js` の `export const CANVAS_HEIGHT = 768;` の直後に追記:

```js

// --- 索敵楕円の基準 ---
// 索敵は「横だけ画面幅に比例する楕円」。横半径は各敵の sightRange
// (= CANVAS_WIDTH * k) をそのまま使い、縦半径はそれに SIGHT_ASPECT を掛ける。
//
// なぜ縦を画面幅に比例させないか:
// 16:9 化は横に広げる方向なので、CANVAS_WIDTH に紐づけたままだと画面の高さは
// 768 で変わらないのに索敵円だけ 1.334 倍になり、「真上・真下の見えない敵に
// 撃たれる」が一律 33% 悪化する。上下移動が主軸のホバー機ではこれが効く。
//
// SIGHT_VERTICAL_BASE を 4:3 時代の幅 (1024) に固定すると、
//   横: 半径/半幅 の比が 4:3 と同じ（0.80 など）
//   縦: 絶対値が 4:3 と同じ（タンクなら 410px）
// の両方が同時に成り立つ。4:3 では SIGHT_ASPECT が厳密に 1.0 になり
// 楕円が真円に退化するので、移行でバランスが動いていないことを証明できる。
//
// ここを CANVAS_WIDTH と同じ値にすると SIGHT_ASPECT = 1 に戻り、
// 「CANVAS_WIDTH を変えただけ」＝縦も等方的に拡大する挙動になる。
// 実機で縦が緩すぎると感じたときの調整点はここ 1 箇所。
export const SIGHT_VERTICAL_BASE = 1024;
export const SIGHT_ASPECT = SIGHT_VERTICAL_BASE / CANVAS_WIDTH;
```

- [ ] **Step 4: `Physics.js` に `withinSight` を足す**

`src/js/utils/Physics.js` の import 文に `CANVAS_WIDTH` は不要。`SIGHT_ASPECT` を足す。
既存の import 行（`TILE_SIZE` を取っている行）に追加する形にすること。

`hasLineOfSight` の閉じ括弧の直後（`pointInRect` の JSDoc の前）に追記:

```js

/**
 * 楕円の索敵判定。`dist < sightRange` の置き換え。
 *
 * 横半径は渡された range そのもの（＝ CANVAS_WIDTH に比例する既存の値）で、
 * 縦半径は range * SIGHT_ASPECT。4:3 では SIGHT_ASPECT === 1 なので真円に
 * 退化し、置き換えの前後で挙動が 1 ドットも変わらない。
 *
 * range に Infinity を渡すと常に true（dx/Infinity が 0 になり、ry も
 * Infinity になるため）。EnemyBase._findTarget の既定引数がこの形。
 *
 * @param {number} dx 標的中心までの横距離
 * @param {number} dy 標的中心までの縦距離
 * @param {number} range 横半径（各敵の sightRange）
 * @returns {boolean}
 */
export function withinSight(dx, dy, range) {
    const ry = range * SIGHT_ASPECT;
    return (dx / range) ** 2 + (dy / ry) ** 2 < 1;
}
```

- [ ] **Step 5: テストが通ることを確かめる**

Run: `npm test -- tests/sight-ellipse.test.js`
Expected: PASS（5 テスト。この時点では `SIGHT_ASPECT === 1` なので skip は出ない）

- [ ] **Step 6: 全テストが緑のままであることを確かめる**

Run: `npm test`
Expected: 全部 PASS。まだ誰も `withinSight` を呼んでいないので、既存の挙動は完全に不変。

- [ ] **Step 7: わざと壊して、テストが赤くなることを確かめる**

`withinSight` の `const ry = range * SIGHT_ASPECT;` を一時的に `const ry = range * 0.5;` に変える。

Run: `npm test -- tests/sight-ellipse.test.js`
Expected: FAIL（「円と楕円が食い違った」と「the vertical radius shrinks by SIGHT_ASPECT」）

**確認したら必ず元に戻すこと。** 戻したあと `npm test -- tests/sight-ellipse.test.js` が PASS することを再確認する。

- [ ] **Step 8: コミット**

```bash
git add src/js/utils/Physics.js src/js/utils/Constants.js tests/sight-ellipse.test.js
git commit -m "feat: 楕円の索敵判定 withinSight を追加（まだ未使用）

横半径は既存の sightRange そのもの、縦半径は SIGHT_ASPECT 倍。
4:3 では SIGHT_ASPECT が 1.0 になり真円に退化するので、この時点で
挙動は 1 ドットも変わらない。境界をまたぐ格子点の総当たりで
既存の円と全一致することを縛った。"
```

---

### Task 2: `EnemyTurret` と `EnemyDrone` の索敵を差し替える

どちらも `dist < range` の単純な置き換え。ドローンだけ緊急防衛の 250px が絡む。

**なぜこの 2 つを同じタスクにするか:** 判定の形が同じ（単一標的に対する真偽値）で、レビューで分けて可否を判断する意味がないため。タンクは順位付けが絡むので別タスクにする。

**`CANVAS_WIDTH` はまだ 1024 のまま。** したがって `SIGHT_ASPECT === 1` で挙動は完全に不変であり、**既存テストが全部緑のままであることが差し替えの正しさの証明になる。**

**Files:**
- Modify: `src/js/entities/EnemyTurret.js:255-259`
- Modify: `src/js/entities/EnemyDrone.js:253-268`
- Test: 既存の `tests/enemy-drone-emergency-defense.test.js`（変更なし。緑のままであることを確認する）

**Interfaces:**
- Consumes: `withinSight(dx, dy, range)`（Task 1）
- Produces: なし（内部実装のみ）

- [ ] **Step 1: `EnemyTurret` の import に `withinSight` を足す**

`src/js/entities/EnemyTurret.js` の `hasLineOfSight` を取っている import 行に `withinSight` を追加する。

- [ ] **Step 2: `EnemyTurret._findTarget` の判定を差し替える**

現在（255〜259 行目付近）:

```js
            const dist = Math.sqrt(dx * dx + dy * dy);

            if (dist < ENEMY_TURRET_SIGHT_RANGE
                && (this.spec.ignoresLineOfSight || this._hasLineOfSight(target))) {
                return target;
            }
```

置き換え後:

```js
            if (withinSight(dx, dy, ENEMY_TURRET_SIGHT_RANGE)
                && (this.spec.ignoresLineOfSight || this._hasLineOfSight(target))) {
                return target;
            }
```

`const dist = ...` の行は他で使われていなければ削除する。**使われていないか必ず確認してから消すこと。**

- [ ] **Step 3: テストを走らせる**

Run: `npm test`
Expected: 全部 PASS。`SIGHT_ASPECT === 1` なので挙動は不変。

- [ ] **Step 4: `EnemyDrone` の import に `withinSight` を足す**

- [ ] **Step 5: `EnemyDrone._findTarget` の判定を差し替える**

現在（253〜268 行目付近）:

```js
            const dx = (target.x + target.width / 2) - (this.x + this.width / 2);
            const dy = (target.y + target.height / 2) - (this.y + this.height / 2);
            const dist = Math.sqrt(dx * dx + dy * dy);

            // Defenders notice approaching players from farther away. (Drones'
            // native sight already exceeds EMERGENCY_DEFENSE_SIGHT_RANGE, so this
            // Math.max never shrinks it — it only widens, and stays correct if the
            // native range is ever lowered.)
            const sightRange = Math.max(
                ENEMY_DRONE_SIGHT_RANGE,
                this.emergencyDefense ? EMERGENCY_DEFENSE_SIGHT_RANGE : 0
            );
            if (dist < sightRange && this._hasLineOfSight(target)) {
                return target;
            }
```

置き換え後:

```js
            const dx = (target.x + target.width / 2) - (this.x + this.width / 2);
            const dy = (target.y + target.height / 2) - (this.y + this.height / 2);

            // 通常の索敵は楕円（横だけ画面幅に比例する）。総攻撃中の緊急索敵
            // 250px は画面比と無関係な「至近距離の反応」なので、楕円に混ぜず
            // 真円のまま OR で足す。Math.max で楕円の横半径ごと広げてしまうと、
            // 16:9 では縦が 250 * SIGHT_ASPECT = 187 に縮んでしまい、
            // 意図しないバランス変更になる。
            // 4:3 では楕円が真円に退化するので、この OR は元の
            // Math.max(sightRange, 250) と完全に同じ結果になる。
            const inSight = withinSight(dx, dy, ENEMY_DRONE_SIGHT_RANGE)
                || (this.emergencyDefense
                    && dx * dx + dy * dy < EMERGENCY_DEFENSE_SIGHT_RANGE ** 2);
            if (inSight && this._hasLineOfSight(target)) {
                return target;
            }
```

- [ ] **Step 6: テストを走らせる**

Run: `npm test`
Expected: 全部 PASS。とくに `tests/enemy-drone-emergency-defense.test.js` が緑であること。

- [ ] **Step 7: わざと壊して、既存テストが差し替えを守れていることを確かめる**

`EnemyDrone` の `withinSight(dx, dy, ENEMY_DRONE_SIGHT_RANGE)` を
`withinSight(dx, dy, 10)` に一時的に変える。

Run: `npm test -- tests/enemy-drone-emergency-defense.test.js`
Expected: FAIL（`idle drone detects the player within native sight` が落ちる）

**確認したら必ず元に戻し、`npm test` が緑に戻ることを再確認する。**

- [ ] **Step 8: コミット**

```bash
git add src/js/entities/EnemyTurret.js src/js/entities/EnemyDrone.js
git commit -m "refactor: タレットとドローンの索敵を withinSight に差し替える

CANVAS_WIDTH はまだ 1024 なので SIGHT_ASPECT === 1、挙動は完全に不変。
既存テストが全部緑のままであることが差し替えの正しさの証明になる。

ドローンの緊急索敵 250px は Math.max ではなく真円の OR にした。
Math.max だと 16:9 で縦が 187px に縮み、意図しないバランス変更になる。"
```

---

### Task 3: `EnemyTank` の索敵を差し替える（順位付けと分離する）

**注意点:** `EnemyTank._findTarget` は「前方 180° の**最近接**」を選ぶために距離の大小比較をしている。単純に真偽値へ差し替えると順位付けが壊れる。

**楕円の内外は `withinSight` で判定し、その中での順位付けは従来どおりユークリッド距離で行う。** 正規化距離（`(dx/rx)² + (dy/ry)²`）で順位付けすると、横に居る敵が不当に優先されてしまう。

**Files:**
- Modify: `src/js/entities/EnemyTank.js:208-232`
- Test: `tests/sight-ellipse.test.js` に順位付けのテストを追加

**Interfaces:**
- Consumes: `withinSight(dx, dy, range)`（Task 1）
- Produces: なし

- [ ] **Step 1: 失敗するテストを書く**

`tests/sight-ellipse.test.js` を編集する。**import は ES modules なのでファイル
先頭にまとめること**（既存の import 群に足す）:

```js
import { EnemyTank } from '../src/js/entities/EnemyTank.js';
import { ENEMY_TANK_SIGHT_RANGE } from '../src/js/utils/Constants.js';
```

そのうえでファイル末尾に追記:

```js

// --- タンクの順位付け ---
// 楕円の内外判定と、その中での「最近接」の順位付けを混ぜないことの回帰。
// 正規化距離で順位を付けると、縦のほうが半径が小さいぶん、横に居る標的が
// 不当に優先される。
//
// 数値の決め方: 横 dx=300 / 縦 dy=262 は「ユークリッド距離では縦が近いのに、
// 正規化距離では横が近くなる」帯 (SIGHT_ASPECT*300 < dy < 300) の内側に
// 取ってある。この帯の外だと、順位付けを壊してもテストが落ちない。

/** 索敵だけを試すための最小のタンク。地形も物理も要らない。 */
function makeTank(x, y) {
  const tank = Object.create(EnemyTank.prototype);
  tank.x = x;
  tank.y = y;
  tank.width = 16;
  tank.height = 12;
  tank.patrolDir = 1; // 右を向いている（前方 180° は dx >= 0 側）
  tank.alive = true;
  return tank;
}

function makeEntity(x, y) {
  return { x, y, width: 16, height: 16, alive: true, docked: false };
}

test('the tank picks the euclidean-nearest target, not the ellipse-nearest', () => {
  const tank = makeTank(0, 0);            // 中心 (8, 6)
  const farHorizontal = makeEntity(300, 0);  // 中心 (308, 8) → dx=300, dy=2
  const nearVertical  = makeEntity(0, 262);  // 中心 (8, 270) → dx=0,   dy=264

  tank.game = { player: farHorizontal, carrier: nearVertical };

  // 両方が索敵の内側にあることが前提。崩れたらテストの意味が無いので明示する。
  assert.ok(withinSight(300, 2, ENEMY_TANK_SIGHT_RANGE), '横の標的は索敵内のはず');
  assert.ok(withinSight(0, 264, ENEMY_TANK_SIGHT_RANGE), '縦の標的は索敵内のはず');

  // ユークリッド距離では 264 < 300 で縦のほうが近い。
  // patrolDir > 0 なので dx >= 0 の標的だけが候補。縦(dx=0)も横(dx=300)も候補。
  assert.equal(
    tank._findTarget(), nearVertical,
    'ユークリッド距離で近いほうが選ばれること'
  );
});

test('the tank ignores targets outside the sight ellipse', () => {
  const tank = makeTank(0, 0);
  const outside = makeEntity(ENEMY_TANK_SIGHT_RANGE + 100, 0);
  tank.game = { player: outside, carrier: null };
  assert.equal(tank._findTarget(), null);
});
```

- [ ] **Step 2: 落ちることを確かめる**

Run: `npm test -- tests/sight-ellipse.test.js`
Expected: 2 つ目（`ignores targets outside`）は現状の実装でも通る可能性がある。
1 つ目が通るか落ちるかは現状の実装次第だが、**このテストの目的は差し替え後に
順位付けが壊れていないことを縛ること**なので、ここで両方 PASS でも構わない。
その場合は Step 6 のミューテーション確認で守れていることを示す。

- [ ] **Step 3: `EnemyTank` の import に `withinSight` を足す**

- [ ] **Step 4: `_findTarget` を差し替える**

現在（208〜232 行目付近）:

```js
        let best    = null;
        let minDist = ENEMY_TANK_SIGHT_RANGE;

        const check = (entity) => {
            if (!entity || !entity.alive) return;
            const dx = (entity.x + entity.width  / 2) - selfCX;
            const dy = (entity.y + entity.height / 2) - selfCY;
            // Only target entities in the forward 180° arc
            if ((this.patrolDir > 0 && dx >= 0) || (this.patrolDir < 0 && dx <= 0)) {
                const dist = Math.sqrt(dx * dx + dy * dy);
                if (dist <= minDist) { minDist = dist; best = entity; }
            }
        };
```

置き換え後:

```js
        let best    = null;
        let minDist = Infinity;

        const check = (entity) => {
            if (!entity || !entity.alive) return;
            const dx = (entity.x + entity.width  / 2) - selfCX;
            const dy = (entity.y + entity.height / 2) - selfCY;
            // Only target entities in the forward 180° arc
            if ((this.patrolDir > 0 && dx >= 0) || (this.patrolDir < 0 && dx <= 0)) {
                // 索敵の内外は楕円で、その中での順位付けはユークリッド距離で。
                // 正規化距離で順位を付けると、縦のほうが半径が小さいぶん
                // 横に居る標的が不当に優先されてしまう。
                if (!withinSight(dx, dy, ENEMY_TANK_SIGHT_RANGE)) return;
                const dist = Math.sqrt(dx * dx + dy * dy);
                if (dist <= minDist) { minDist = dist; best = entity; }
            }
        };
```

**`minDist` の初期値が `ENEMY_TANK_SIGHT_RANGE` から `Infinity` に変わっている点に注意。**
射程の足切りが `withinSight` に移ったので、`minDist` は純粋に順位付けだけを担う。

- [ ] **Step 5: テストを走らせる**

Run: `npm test`
Expected: 全部 PASS。

- [ ] **Step 6: わざと壊して、テストが赤くなることを確かめる**

`const dist = Math.sqrt(dx * dx + dy * dy);` を、正規化距離
`const dist = Math.hypot(dx / ENEMY_TANK_SIGHT_RANGE, dy / (ENEMY_TANK_SIGHT_RANGE * SIGHT_ASPECT));`
に一時的に変える（`SIGHT_ASPECT` の import が要る）。

Run: `npm test -- tests/sight-ellipse.test.js`
Expected: 4:3 では `SIGHT_ASPECT === 1` なので**落ちない**（正規化距離が
ユークリッド距離の定数倍になり、順位が変わらないため）。
**`SIGHT_ASPECT` の代わりに `0.75` を直書きして試し直すこと。**
そうすると横 300 の正規化距離 0.732 に対し縦 264 が 0.859 となって順位が
逆転し、`the tank picks the euclidean-nearest target` が落ちる。

（Task 6 で `CANVAS_WIDTH` を 1366 にしたあとは `SIGHT_ASPECT` が 0.7496 に
なるので、直書きなしでこのミューテーションが検出できるようになる）

**確認したら必ず元に戻し、`npm test` が緑に戻ることを再確認する。**

- [ ] **Step 7: コミット**

```bash
git add src/js/entities/EnemyTank.js tests/sight-ellipse.test.js
git commit -m "refactor: タンクの索敵を withinSight に差し替える

前方180°の最近接を選ぶので、楕円の内外判定と順位付けを分けた。
順位付けは従来どおりユークリッド距離。正規化距離だと縦の半径が
小さいぶん横に居る標的が不当に優先される。
射程の足切りが withinSight に移ったので minDist の初期値は Infinity。"
```

---

### Task 4: `EnemyAttacker` の索敵を差し替える

アタッカーは発砲を `aiState === 'chase'` でゲートしていて（`attacker/combat.js:50`）、その state は `EnemyAttacker.update()` の索敵判定 1 箇所で決まる。**触るのはそこだけでよい。**

**Files:**
- Modify: `src/js/entities/EnemyAttacker.js:151-160`
- Test: 既存の `tests/enemy-attacker-emergency-defense.test.js` / `tests/emergency-wild-fire.test.js`（変更なし。緑のままであることを確認する）

**Interfaces:**
- Consumes: `withinSight(dx, dy, range)`（Task 1）
- Produces: なし

- [ ] **Step 1: `EnemyAttacker` の import に `withinSight` を足す**

`src/js/utils/Physics.js` からの import 行に追加する。既に Physics から何か取っていればその行に、無ければ新しく import 文を足す。

- [ ] **Step 2: `update()` の索敵判定を差し替える**

現在（151〜160 行目付近）:

```js
        const target = this._getClosestTarget();
        const targetDist = target ? this._distToTarget(target) : Infinity;

        // --- AI state decision ---
        this.currentTarget = target;
        // While defending the base, notice approaching players from farther away.
        const sightRange = this.emergencyDefense
            ? Math.max(this.config.sightRange, EMERGENCY_DEFENSE_SIGHT_RANGE)
            : this.config.sightRange;
        if (target && targetDist <= sightRange) {
```

置き換え後:

```js
        const target = this._getClosestTarget();
        const targetDist = target ? this._distToTarget(target) : Infinity;

        // --- AI state decision ---
        this.currentTarget = target;
        // 通常の索敵は楕円（横だけ画面幅に比例する）。総攻撃中の緊急索敵 250px は
        // 画面比と無関係な「至近距離の反応」なので、楕円に混ぜず真円のまま OR で
        // 足す。Math.max で楕円の横半径ごと広げると、16:9 では縦が
        // 250 * SIGHT_ASPECT = 187 に縮み、意図しないバランス変更になる。
        // 4:3 では楕円が真円に退化するので、この OR は元の
        // Math.max(config.sightRange, 250) と完全に同じ結果になる。
        const dx = target ? (target.x + target.width / 2) - (this.x + this.width / 2) : 0;
        const dy = target ? (target.y + target.height / 2) - (this.y + this.height / 2) : 0;
        const inSight = !!target && (
            withinSight(dx, dy, this.config.sightRange)
            || (this.emergencyDefense && targetDist < EMERGENCY_DEFENSE_SIGHT_RANGE)
        );
        if (inSight) {
```

`targetDist` は `_distToTarget()`（ユークリッド距離）なので、緊急索敵の真円判定に
そのまま使える。`targetDist` は下流でも使われているので**消さないこと。**

`EMERGENCY_DEFENSE_SIGHT_RANGE` の import は既にあるので変更不要。

- [ ] **Step 3: テストを走らせる**

Run: `npm test`
Expected: 全部 PASS。とくに以下が緑であること:
- `tests/enemy-attacker-emergency-defense.test.js`
- `tests/emergency-wild-fire.test.js`
- `tests/base-emergency-defense-full-chain.test.js`
- `tests/attacker-leg-animation.test.js`

これらは偽 config に `sightRange: 100` を直接書いている。`withinSight` が
既存の `sightRange` をそのまま横半径として受け取る設計なので、修正は不要のはず。

- [ ] **Step 4: わざと壊して、既存テストが差し替えを守れていることを確かめる**

`withinSight(dx, dy, this.config.sightRange)` を `withinSight(dx, dy, 1)` に
一時的に変える。

Run: `npm test`
Expected: FAIL（アタッカーが自機を見つけられなくなり、chase に入るテストが落ちる）

**落ちるテストが 1 本も無い場合は、索敵が chase に入ることを縛るテストが
存在しないということなので、Step 5 に進む前に報告すること。**

**確認したら必ず元に戻し、`npm test` が緑に戻ることを再確認する。**

- [ ] **Step 5: コミット**

```bash
git add src/js/entities/EnemyAttacker.js
git commit -m "refactor: アタッカーの索敵を withinSight に差し替える

発砲は aiState === 'chase' でゲートされていて、その state は
update() の索敵判定 1 箇所で決まるので、触るのはそこだけでよい。

緊急索敵 250px はドローンと同じく真円の OR にした。"
```

---

### Task 5: `EnemyBase._findTarget` の距離判定を差し替える

基地レーザーの索敵。`_findTarget(maxRange)` は 4 箇所から `BASE_LASER_RANGE` 付きで呼ばれている。**引数の意味は「横半径」のまま変わらない**ので、呼び出し側 4 箇所は触らない。

**Files:**
- Modify: `src/js/entities/EnemyBase.js:367-388`
- Test: 既存の `tests/base-emergency-defense.test.js` / `tests/base-emergency-defense-full-chain.test.js`（変更なし）

**Interfaces:**
- Consumes: `withinSight(dx, dy, range)`（Task 1）
- Produces: `_findTarget(maxRange?: number)` — シグネチャは不変

- [ ] **Step 1: `EnemyBase` の import に `withinSight` を足す**

- [ ] **Step 2: `_findTarget` の判定を差し替える**

現在（373〜387 行目付近）:

```js
        let bestTarget = null;
        let minDist = maxRange;

        const centerX = this.x + this.width / 2;
        const centerY = this.y + this.height / 2;

        for (const c of candidates) {
            const dx = c.x + c.width / 2 - centerX;
            const dy = c.y + c.height / 2 - centerY;
            const dist = Math.sqrt(dx * dx + dy * dy);
            if (dist < minDist) {
                minDist = dist;
                bestTarget = c;
            }
        }
        return bestTarget;
```

置き換え後:

```js
        let bestTarget = null;
        let minDist = Infinity;

        const centerX = this.x + this.width / 2;
        const centerY = this.y + this.height / 2;

        for (const c of candidates) {
            const dx = c.x + c.width / 2 - centerX;
            const dy = c.y + c.height / 2 - centerY;
            // タンクと同じく、索敵の内外は楕円で、順位付けはユークリッド距離で。
            // maxRange の既定は Infinity で、withinSight はそのとき常に true を返す。
            if (!withinSight(dx, dy, maxRange)) continue;
            const dist = Math.sqrt(dx * dx + dy * dy);
            if (dist < minDist) {
                minDist = dist;
                bestTarget = c;
            }
        }
        return bestTarget;
```

**`minDist` の初期値が `maxRange` から `Infinity` に変わっている点に注意。**
射程の足切りが `withinSight` に移った。

- [ ] **Step 3: テストを走らせる**

Run: `npm test`
Expected: 全部 PASS。

- [ ] **Step 4: 索敵の差し替えが全部終わったことを確かめる**

`dist < ...SIGHT_RANGE` / `dist <= sightRange` 相当の判定が残っていないことを
目視で確認する（**grep でテストを書くのではなく、作業の確認としての grep**）:

```bash
grep -rn "SIGHT_RANGE\|sightRange" src/js/entities/
```

`EMERGENCY_DEFENSE_SIGHT_RANGE` の真円 OR と、`withinSight` に渡している箇所
以外に、距離の直接比較が残っていないこと。

- [ ] **Step 5: コミット**

```bash
git add src/js/entities/EnemyBase.js
git commit -m "refactor: 基地レーザーの索敵を withinSight に差し替える

_findTarget(maxRange) の引数の意味は横半径のまま。呼び出し側4箇所は不変。
射程の足切りが withinSight に移ったので minDist の初期値は Infinity。

これで索敵5箇所すべてが楕円判定になった。CANVAS_WIDTH はまだ 1024 なので
挙動は完全に不変で、既存テストが全部緑のままであることがその証明になる。"
```

---

### Task 6: `CANVAS_WIDTH` を 1366 にする

**ここで初めて挙動が変わる。** 前タスクまでで楕円化は完了していて挙動は不変だったので、ここから先に出る差分はすべて「画面比の影響」であり、「楕円化のバグ」と混ざらない。

**Files:**
- Modify: `src/js/utils/Constants.js:5`
- Modify: `src/style.css`
- Modify: `index.html`
- Modify: 期待値が動く既存テスト（下記の手順で洗い出す）

**Interfaces:**
- Consumes: なし
- Produces: `CANVAS_WIDTH = 1366`、`SIGHT_ASPECT = 1024 / 1366 = 0.74963...`

- [ ] **Step 1: `CANVAS_WIDTH` を変える**

`src/js/utils/Constants.js:5`:

```js
// 16:9。高さ 768 を保つ真の 16:9 は幅 1365.33 で整数にならないので、
// WXGA の実標準 1366 を採る（1366/768 = 1.7786、16:9 の 1.7778 との差は 0.05%）。
// style.css の黒帯計算をこの比で書けば歪みは出ない。
// TILE_SIZE(16) の倍数である必要はない。カメラは px 単位の任意位置を取り、
// 地形描画は startCol/endCol で切っているため。
export const CANVAS_WIDTH = 1366;
```

- [ ] **Step 2: テストを走らせて、動くものを洗い出す**

Run: `npm test`
Expected: いくつか FAIL する。**落ちたテストを全部記録してから直すこと。**

`CANVAS_WIDTH` / `CANVAS_HEIGHT` を参照しているテストは以下（要確認）:
`tests/audio-panning.test.js`, `tests/audio-falloff.test.js`,
`tests/cave-backdrop.test.js`, `tests/destruction-finale.test.js`,
`tests/volume-indicator.test.js`, `tests/smoke-autoaim.test.js`,
`tests/smoke-concealment.test.js`, `tests/smoke-screen.test.js`,
`tests/enemy-drone-emergency-defense.test.js`

- [ ] **Step 3: 落ちたテストを「定数から導く」形に直す**

**ハードコードされた数値が原因なら、その場で定数由来に直す。** 例:

```js
// 直す前（1024 を直書きしている）
assert.equal(AUDIO_PAN_RANGE, 512);

// 直したあと（定数から導く）
assert.equal(AUDIO_PAN_RANGE, CANVAS_WIDTH / 2);
```

**期待値をただ新しい数値に書き換えるだけにしないこと。** 次に解像度が変わったとき同じ作業が発生する。

`tests/sight-ellipse.test.js` の `at SIGHT_ASPECT === 1` は `t.skip()` に入る。
これは想定どおりで、真円への退化は Task 1 のコミット時点で証明済み。

- [ ] **Step 4: テストが緑になることを確かめる**

Run: `npm test`
Expected: 全部 PASS

- [ ] **Step 5: `style.css` の黒帯計算を直す**

`src/style.css` の `canvas` ルール。現在:

```css
    width: min(100vw, 133.334vh);
    height: min(75vw, 100vh);
```

置き換え後（コメント内の「4:3」「1024x768」も 16:9 の値に直すこと）:

```css
    /* 内部解像度 1366x768 は据え置きのまま、表示だけ 16:9 を保って最大化する。
       177.86vh = 100vh * 1366/768、56.22vw = 100vw * 768/1366。
       幅と高さの両方に min() を掛けると、横長の画面では高さが、
       縦長の画面では幅が制約になり、どちらでも比率が崩れない。 */
    width: min(100vw, 177.86vh);
    height: min(56.22vw, 100vh);
```

**既存コメントの後半（`AUTO_AIM_CANCEL_THRESHOLD_DEFAULT` に触れている段落）は
そのまま残すこと。** 拡大率が変わるとキャンセルに要るマウス移動量が変わるという
指摘は 16:9 でも有効で、実機確認の項目になっている。

- [ ] **Step 6: `index.html` の canvas 属性を合わせる**

現在 `<canvas id="gameCanvas" width="800" height="600">`。
`main.js:178-179` が起動時に `CANVAS_WIDTH`/`CANVAS_HEIGHT` で上書きするので
実害は無いが、読んだ人が誤解するので合わせる:

```html
        <canvas id="gameCanvas" width="1366" height="768"></canvas>
```

- [ ] **Step 7: コミット**

```bash
git add src/js/utils/Constants.js src/style.css index.html tests/
git commit -m "feat: 内部解像度を 1366x768 (16:9) にする

高さ 768 を保つ真の 16:9 は幅 1365.33 で整数にならないので、WXGA の
実標準 1366 を採る（16:9 との差 0.05%）。

索敵は前タスクまでで楕円化済みなので、横は「半径/半幅」の比が 4:3 と
同じまま、縦は絶対値が 4:3 と同じまま移行する。

落ちたテストは期待値を書き換えるのではなく、CANVAS_WIDTH から導く形に
直した。次に解像度を変えたとき同じ作業が起きないように。"
```

**`git add tests/` はテストディレクトリ全体になる。`git status` で意図しない
ファイルが入っていないか確認してから commit すること。`src/js/main.js` が
混ざっていないことを必ず見ること（debugStartMission が入る）。**

---

### Task 7: 弾の寿命を横の伸び率に合わせて伸ばす

発砲のゲートは索敵だけで、別途の射程チェックが無い。索敵の横半径を 1.334 倍にしたので、弾の射程が追いつかないと「撃たれるが弾が目の前で消える帯」ができる。

**縦の理不尽は増えない。** 縦の索敵が 4:3 の絶対値に据え置かれている以上、敵は縦 410px の外からは撃ってこない。弾が縦に 720px 飛べるようになっても、そもそも撃たれない。

**Files:**
- Modify: `src/js/utils/Constants.js:148, 171, 282, 286`
- Test: `tests/aspect-16-9.test.js`（次のタスクで作る。ここでは定数の変更だけ）

**Interfaces:**
- Consumes: なし
- Produces: `MISSILE_LIFETIME = 240`, `PLAYER_MG_LIFETIME = 240`, `ENEMY_BULLET_LIFETIME = 240`, `ENEMY_HOMING_MISSILE_LIFETIME = 400`

- [ ] **Step 1: 4 つの定数を変える**

`src/js/utils/Constants.js:148`:

```js
// 16:9 化で索敵の横半径が 1.334 倍になったぶん、射程も同じ倍率で伸ばす。
// 180 * 1.334 = 240.1。速度 6 なので射程 1080 -> 1440px。
// 画面半幅 683 に対する比 2.11 は 4:3 のときと同じ。
export const MISSILE_LIFETIME = 240; // frames
```

`src/js/utils/Constants.js:171`:

```js
// 16:9 化に合わせて 180 -> 240（元は 240 で、4:3 のとき 80% に詰めた経緯がある）。
// 速度 4 なので射程 720 -> 960px。画面半幅 683 に対する比 1.41 は 4:3 と同じ。
export const PLAYER_MG_LIFETIME = 240;
```

`src/js/utils/Constants.js:282`:

```js
// 16:9 化で伸ばした。速度 3 なので射程 540 -> 720px。
// 4:3 ではタレットの索敵 512 に対し弾 540 と 5% しか余裕が無く、手で詰めた値
// だった。16:9 のタレット索敵は横 683 なので、720 で足りる。
// なお敵のうち EnemyBullet を撃つのはタンク・タレット・ドローンだけで、
// アタッカーは素の Missile を、artillery は EnemyHomingMissile を撃つ。
export const ENEMY_BULLET_LIFETIME = 240;    // frames (4s)
```

`src/js/utils/Constants.js:286`:

```js
// 16:9 化で伸ばした。300 * 1.334 = 400.2。速度 3 なので射程 900 -> 1200px。
// artillery の索敵は横 1093 なので、これで届く（4:3 では 819 vs 900 だった）。
export const ENEMY_HOMING_MISSILE_LIFETIME = 400; // Lives longer to find target
```

- [ ] **Step 2: テストを走らせる**

Run: `npm test`
Expected: 全部 PASS。落ちるものがあれば、寿命をハードコードしているテストなので
定数由来に直す。

- [ ] **Step 3: コミット**

```bash
git add src/js/utils/Constants.js
git commit -m "feat: 弾の寿命を 16:9 の横の伸び率に合わせて伸ばす

発砲のゲートは索敵だけで別途の射程チェックが無いので、索敵の横半径を
1.334 倍にしたぶん射程も伸ばさないと「撃たれるが弾が届かない帯」ができる。

縦の理不尽は増えない。縦の索敵が 4:3 の絶対値に据え置かれているので、
敵は縦 410px の外からは撃ってこない。"
```

---

### Task 8: 16:9 側の不変条件をテストで縛る

「横の力関係も縦の力関係も 4:3 のまま保存されている」ことと、「弾が索敵をカバーしている」ことを回帰として残す。

**Files:**
- Test: `tests/aspect-16-9.test.js`（新規）

**Interfaces:**
- Consumes: `Constants.js` の索敵・寿命の各定数、`SIGHT_ASPECT`
- Produces: なし

- [ ] **Step 1: テストを書く**

`tests/aspect-16-9.test.js` を新規作成:

```js
import { test } from 'node:test';
import assert from 'node:assert/strict';
import { withinSight } from '../src/js/utils/Physics.js';
import {
  CANVAS_WIDTH, CANVAS_HEIGHT, SIGHT_VERTICAL_BASE, SIGHT_ASPECT,
  EMERGENCY_DEFENSE_SIGHT_RANGE,
  ENEMY_TANK_SIGHT_RANGE, ENEMY_TURRET_SIGHT_RANGE, ENEMY_DRONE_SIGHT_RANGE,
  BASE_LASER_RANGE, ENEMY_ATTACKER_TYPES,
  ENEMY_BULLET_SPEED, ENEMY_BULLET_LIFETIME,
  MISSILE_SPEED, MISSILE_LIFETIME,
  ENEMY_HOMING_MISSILE_MAX_SPEED, ENEMY_HOMING_MISSILE_LIFETIME,
  PLAYER_MG_SPEED, PLAYER_MG_LIFETIME,
} from '../src/js/utils/Constants.js';

// 4:3 時代の実測値。索敵の縦の絶対値と、横の「半径/半幅」の比が
// これらから動いていないことを縛る。
const BASE_WIDTH = 1024;
const BASE_HALF_W = BASE_WIDTH / 2;                       // 512

/** 各敵の索敵係数 k（sightRange = CANVAS_WIDTH * k）。 */
const SIGHT_COEFFICIENTS = {
  tank:      ENEMY_TANK_SIGHT_RANGE   / CANVAS_WIDTH,
  turret:    ENEMY_TURRET_SIGHT_RANGE / CANVAS_WIDTH,
  drone:     ENEMY_DRONE_SIGHT_RANGE  / CANVAS_WIDTH,
  baseLaser: BASE_LASER_RANGE         / CANVAS_WIDTH,
  ...Object.fromEntries(
    Object.entries(ENEMY_ATTACKER_TYPES)
      .map(([name, cfg]) => [name, cfg.sightRange / CANVAS_WIDTH])
  ),
};

// --- 横の力関係の保存 ---

test('the horizontal sight-to-halfwidth ratio is unchanged from 4:3', () => {
  const halfW = CANVAS_WIDTH / 2;
  for (const [name, k] of Object.entries(SIGHT_COEFFICIENTS)) {
    const now = (CANVAS_WIDTH * k) / halfW;
    const before = (BASE_WIDTH * k) / BASE_HALF_W;
    assert.equal(now, before, `${name}: 横の比が 4:3 から動いている`);
  }
});

// --- 縦の力関係の保存 ---

test('the vertical sight radius keeps its 4:3 absolute value', () => {
  for (const [name, k] of Object.entries(SIGHT_COEFFICIENTS)) {
    const ry = (CANVAS_WIDTH * k) * SIGHT_ASPECT;
    assert.equal(
      Math.round(ry), Math.round(BASE_WIDTH * k),
      `${name}: 縦の絶対値が 4:3 から動いている`
    );
  }
});

test('SIGHT_VERTICAL_BASE is the 4:3 width, so the vertical side never moved', () => {
  assert.equal(SIGHT_VERTICAL_BASE, BASE_WIDTH);
});

// --- 画面比 ---

test('the canvas is 16:9 within a tenth of a percent', () => {
  const ratio = CANVAS_WIDTH / CANVAS_HEIGHT;
  assert.ok(
    Math.abs(ratio - 16 / 9) / (16 / 9) < 0.001,
    `${CANVAS_WIDTH}x${CANVAS_HEIGHT} は 16:9 から離れすぎ (${ratio})`
  );
});

// --- 射程が索敵をカバーしているか ---
//
// 発砲のゲートは索敵だけで、別途の射程チェックが無い。索敵の横半径より
// 弾の射程が短いと「撃たれるが弾が目の前で消える帯」ができる。

const RANGE_COVERAGE = [
  // [名前, 索敵の横半径, 弾の射程]
  ['tank',      ENEMY_TANK_SIGHT_RANGE,   ENEMY_BULLET_SPEED * ENEMY_BULLET_LIFETIME],
  ['turret',    ENEMY_TURRET_SIGHT_RANGE, ENEMY_BULLET_SPEED * ENEMY_BULLET_LIFETIME],
  // アタッカーは EnemyBullet ではなく素の Missile を撃つ（attacker/combat.js:_fire）
  ['standard',  ENEMY_ATTACKER_TYPES.standard.sightRange, MISSILE_SPEED * MISSILE_LIFETIME],
  ['rival',     ENEMY_ATTACKER_TYPES.rival.sightRange,    MISSILE_SPEED * MISSILE_LIFETIME],
  ['heavy',     ENEMY_ATTACKER_TYPES.heavy.sightRange,    MISSILE_SPEED * MISSILE_LIFETIME],
  // artillery だけ EnemyHomingMissile
  ['artillery', ENEMY_ATTACKER_TYPES.artillery.sightRange,
                ENEMY_HOMING_MISSILE_MAX_SPEED * ENEMY_HOMING_MISSILE_LIFETIME],
];

for (const [name, sight, reach] of RANGE_COVERAGE) {
  test(`${name}: the projectile reaches the edge of its own sight`, () => {
    assert.ok(reach >= sight, `索敵 ${sight}px に対し射程 ${reach}px しかない`);
  });
}

// ドローンは既知の例外。4:3 の時点で索敵 717 に対し弾 540 と 177px 足りて
// いない。実害が無いのは、索敵範囲いっぱいから撃たずに
// ENEMY_DRONE_HOVER_DIST_X = 180 / _Y = 120 のホバー定位置まで寄ってから
// 撃つため（実効の交戦距離は約 216px）。既存の設計なので直さない。
test('the drone is the documented exception: its bullet does not reach its sight', () => {
  const reach = ENEMY_BULLET_SPEED * ENEMY_BULLET_LIFETIME;
  assert.ok(
    reach < ENEMY_DRONE_SIGHT_RANGE,
    'ドローンが例外でなくなったなら、上のコメントごと見直すこと'
  );
});

// --- 緊急防衛の真円が、楕円の外側でも効くこと ---
//
// 総攻撃中の緊急索敵 250px は画面比と無関係な「至近距離の反応」なので、
// 楕円に混ぜず真円のまま OR で足してある（EnemyDrone / EnemyAttacker）。
// 16:9 では楕円の縦が縮むので、「楕円の外・真円の内」という点が実在する。
// そこで OR が効いていなければ、総攻撃中の敵が真上の自機を見落とす。

test('the 250px emergency circle covers points the sight ellipse misses', () => {
  const range = 100;                       // テストの偽 config が使っている値
  const ry = range * SIGHT_ASPECT;
  const dy = EMERGENCY_DEFENSE_SIGHT_RANGE - 10;  // 240: 真円の内側

  assert.ok(dy > ry, `楕円の縦半径 ${ry} より外に取れていない`);
  assert.equal(withinSight(0, dy, range), false, '楕円の外であること');
  assert.ok(
    dy * dy < EMERGENCY_DEFENSE_SIGHT_RANGE ** 2,
    '真円の内であること'
  );
});

// --- 自機側 ---

test('player weapons still outrange half the screen as they did at 4:3', () => {
  const halfW = CANVAS_WIDTH / 2;
  // 4:3 での比: ミサイル 2.11、MG 1.41
  assert.ok((MISSILE_SPEED * MISSILE_LIFETIME) / halfW >= 2.0, 'ミサイルの射程が縮んだ');
  assert.ok((PLAYER_MG_SPEED * PLAYER_MG_LIFETIME) / halfW >= 1.3, 'MG の射程が縮んだ');
});
```

- [ ] **Step 2: テストが通ることを確かめる**

Run: `npm test -- tests/aspect-16-9.test.js`
Expected: 全部 PASS

- [ ] **Step 3: わざと壊して、赤くなることを確かめる**

3 つ試して、それぞれ想定どおりのテストだけが落ちることを確認する。
**1 つ試すごとに元に戻すこと。**

1. `Constants.js` の `SIGHT_VERTICAL_BASE` を 1366 にする
   → `the vertical sight radius keeps its 4:3 absolute value` と
     `SIGHT_VERTICAL_BASE is the 4:3 width` が落ちる
2. `ENEMY_BULLET_LIFETIME` を 180 に戻す
   → `turret: the projectile reaches the edge of its own sight` が落ちる
3. `ENEMY_HOMING_MISSILE_LIFETIME` を 300 に戻す
   → `artillery: the projectile reaches the edge of its own sight` が落ちる
4. `EnemyAttacker.update()` の緊急索敵の OR（`|| (this.emergencyDefense && ...)`）を
   消す → `tests/enemy-attacker-emergency-defense.test.js` が落ちる
   （`the 250px emergency circle covers points the sight ellipse misses` は
   幾何だけを見ているので落ちない。**実装を守っているのは既存テストのほう**
   だと確認しておく）

- [ ] **Step 4: 全テストを走らせる**

Run: `npm test`
Expected: 全部 PASS

- [ ] **Step 5: コミット**

```bash
git add tests/aspect-16-9.test.js
git commit -m "test: 16:9 の不変条件を縛る

横の「索敵/半幅」の比と縦の索敵の絶対値が、どちらも 4:3 から
動いていないこと。弾の射程が自分の索敵をカバーしていること。
ドローンだけは 4:3 の時点からの既知の例外なので、例外であること
自体をテストにして、解消されたらコメントを見直せるようにした。"
```

---

### Task 9: ユーザーへの引き渡し

実機での見た目・音の確認は必ずユーザーが行う。ローカルサーバーはユーザーが IDE 側で常時立てているので、こちらでは起動しない。

**Files:** なし（変更なし）

**Interfaces:**
- Consumes: Task 1〜8 の全成果
- Produces: なし

- [ ] **Step 1: 最終確認**

```bash
npm test
git status --short
git log --oneline main..HEAD
```

- `npm test` が全部 PASS すること
- `git status --short` に `src/js/main.js` の M だけが残っていること
  （ユーザーの `debugStartMission` は未コミットのまま）
- 意図しないファイルがコミットされていないこと

- [ ] **Step 2: ユーザーに確認ポイントを渡す**

以下を伝える。**ハードリロード（Cmd+Shift+R）が要ることを必ず言う**
（`index.html` が `main.js?v=1.0` でキャッシュを効かせているため、
忘れると「効いていない」と誤解される）。

**見てほしいところ:**

| 項目 | 見どころ |
|---|---|
| 画面の縦横比 | 黒帯が出ずに 16:9 で最大化されるか。ウィンドウを縦長・横長にしても崩れないか |
| 各画面のレイアウト | タイトル / ランキング / 設定 / 面セレクト。パネルは中央寄せなので無傷のはずだが、`ui/screens/layout.js` は実機で見て決めた値 |
| 縦の索敵 | 真上・真下の敵に撃たれる感じが 4:3 と変わっていないか（**ここが今回の設計の要**） |
| 横の索敵 | 画面端に見えている敵が、こちらを見つけるタイミングが 4:3 と変わっていないか |
| 弾が届くか | 画面端の敵に自機の弾が届くか。敵の弾が手前で消えないか |
| 遮蔽 | 横視界が 1.334 倍になったぶん、視線が地形に遮られる確率が上がる。「見えているのに撃ってこない」が増えていないか |
| オートエイム解除 | CSS の拡大率が変わったので、解除に要るマウスの移動量が変わっている（`AUTO_AIM_CANCEL_THRESHOLD_DEFAULT`） |
| マップの広さ | 最小マップ幅 2400px に対し視界 1366px ＝ 57% が一度に見える。広すぎないか |

**調整用の定数の対応表:**

| 感じたこと | 変える定数 | 現在値 |
|---|---|---|
| 縦の索敵がきつい | `SIGHT_VERTICAL_BASE` を下げる | 1024 |
| 縦の索敵が緩い（4:3 と同じ等方拡大に戻す） | `SIGHT_VERTICAL_BASE` を 1366 に | 1024 |
| 敵の弾が手前で消える | `ENEMY_BULLET_LIFETIME` | 240 |
| 自機の MG が届かない | `PLAYER_MG_LIFETIME` | 240 |
| 自機のミサイルが届かない | `MISSILE_LIFETIME` | 240 |
| ホーミングが届かない | `ENEMY_HOMING_MISSILE_LIFETIME` | 400 |
| 画面が横に広すぎる | `CANVAS_WIDTH`（`style.css` の比も要変更） | 1366 |
| オートエイムの解除が敏感/鈍い | `AUTO_AIM_CANCEL_THRESHOLD_DEFAULT` | — |

**保留にしていること:** `MIN_MAP_COLS`（最小マップ幅）。マップ生成の途中で
`game.rng` の消費を変えると敵の構成が変わり、週次の決定性が壊れる
（`tests/MapDeterminism.test.js` の回帰あり）。触るなら別タスクとして立てる。

- [ ] **Step 3: フィードバックを受けて調整する**

ユーザーの実機確認の結果に応じて、上の表の定数を調整する。
**バランス調整は根本の作り直しより低リスクな数値調整を優先する。**
数値を決めたら、その根拠（実機でどう感じたか、どの値を経て確定したか）を
コメントに残す。
