# archive

**使っていないものを置く場所。ここのファイルはゲーム本体から一切参照されていない。**

現役の道具は `tools/`（node で走らせて音を WAV に書き出す）。以前この archive が
`tool/` という名前で、`tools/` と1文字違いで並んでいたため、どちらが生きているのか
毎回調べる羽目になっていた。それで改名した（2026-08-16）。

## ShapeInspector_*.js

機体の描画コードを貼ると canvas に描いて確かめられるエディタ。React ＋
lucide-react の単体アプリで、**このリポジトリでは動かせない**（ゲーム本体は
ビルド工程なし・依存パッケージなしという方針なので、React も lucide-react も
入っていない）。Google AI Studio 側で作ったものを手で置いた名残。

最終更新は 2026-05-11、コミットは `f3c1116` の1本だけ。その後の脚アニメーション
刷新にも破壊演出にも使われていない。`検索あり` / `検索なし` は同じものの版違い。

**消してよい。** 履歴に残るので `git show f3c1116` で戻せる。
